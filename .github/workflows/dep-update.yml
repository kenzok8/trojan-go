name: dep-update

on:
  workflow_dispatch:
    inputs:
      packages:
        description: 'Go packages to update (space-separated, e.g. "github.com/v2fly/v2ray-core/v4@v4.45.2"). Leave empty to run go get -u ./...'
        required: false
        default: ''

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Update dependencies
        run: |
          if [ -n "${{ github.event.inputs.packages }}" ]; then
            for pkg in ${{ github.event.inputs.packages }}; do
              go get "$pkg"
            done
          else
            go get -u ./...
          fi
          # Use -go=1.24 to pin the go directive for OpenWrt SDK (Go 1.24.13 + GOTOOLCHAIN=local) compatibility
          # This sets the directive AND regenerates go.sum in one consistent step
          go mod tidy -go=1.24.0

      - name: Verify build (catch API breakage)
        run: go build -tags full ./...

      - name: Show diff
        run: git diff go.mod go.sum

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add go.mod go.sum
          git diff --cached --exit-code || git commit -m "deps: update go modules via dep-update workflow"
          git push